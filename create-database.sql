create database `nataly`;
# A linha cima serve para criar um banco de dados com o nome: `Nataly`
# O uso da crase se torna necessário para indicar uma `string` do sistema
# `String` do sistema: `datbase`, `table` e `campo`